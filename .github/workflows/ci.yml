name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  CMAKE_GENERATOR: Ninja

jobs:
  test:
    name: Test (${{ matrix.arch }}, llm)
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: windows-x64
            runner: windows-latest
          - arch: windows-arm64
            runner: windows-11-arm

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Windows build dependencies
        uses: ilammy/msvc-dev-cmd@v1

      - name: Setup CMake (Windows)
        uses: lukka/get-cmake@latest

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target
          shared-key: ${{ matrix.arch }}-cpu-v5-llm

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Typecheck frontend
        run: npx tsc --noEmit

      # `llm` is a default feature and is core to Butchi's translate/rewrite path.
      # Keep default features enabled so CI compiles llama-cpp-2 and hf-hub.
      - name: Cargo check (llm)
        working-directory: src-tauri
        run: cargo check

      - name: Cargo test (llm)
        working-directory: src-tauri
        run: cargo test -- --nocapture

      - name: Cargo clippy (llm)
        working-directory: src-tauri
        run: cargo clippy --all-targets -- -D warnings
        continue-on-error: true
